# ============================================================
# Streamlit 应用运行时依赖（Streamlit Community Cloud 会读这个文件安装）
# 说明：这里只放「跑 Streamlit 问数应用」必需的最小集合，保证云端安装又快又稳。
#      编排/MCP/API/测试等开发与进阶依赖见 requirements-dev.txt：
#          pip install -r requirements.txt -r requirements-dev.txt
# ============================================================

sqlglot>=25.0            # SQL AST 解析与方言校验（nl2sql/validation.py）
pydantic>=2.0            # 配置与数据模型
pydantic-settings>=2.0   # 配置加载（环境变量 / .env / Streamlit secrets）
python-dotenv>=1.0       # pydantic-settings 读取 env_file=".env" 所需
openai>=1.30             # OpenAI 兼容 LLM 客户端（通义/DeepSeek/Ollama 均可）
httpx>=0.27              # 显式依赖：直接构造"绕开环境代理"的 http_client
psycopg[binary]>=3.1     # 真实 PostgreSQL 预检与执行（Neon，支持 pgvector/pg_trgm）
streamlit>=1.36          # Web UI 入口（streamlit_app.py）
