# 核心依赖（必装）
sqlglot>=25.0          # SQL AST 解析与方言校验
pydantic>=2.0          # 配置模型
pydantic-settings>=2.0 # .env 配置加载
openai>=1.30           # OpenAI 兼容 LLM 客户端（通义/DeepSeek/Ollama 均可）

# 真实 PostgreSQL 预检（生产启用；不装则自动回退到 Mock）
psycopg[binary]>=3.1

# 开发依赖（测试）
pytest>=8.0
